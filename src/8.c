// FinalProject.c

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  int number = 12; // Change this to a different integer value
  char* str = "Hello, World!";
  printf("%d %s\n", number, str);
  return 0;
}
