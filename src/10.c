#include <stdio.h>
#include <stdlib.h>

int main() {
    int x = 5;
    float y = 3.14;
    char z = 'a';
    printf("The value of x is %d, the value of y is %f, and the value of z is %c\n", x, y, z);
    return 0;
}
