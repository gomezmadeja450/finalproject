#include <stdio.h>
#include <stdlib.h>

int main() {
    int x = 0;
    int y = 123456789;
    if (x > y) {
        printf("x is greater than y\n");
    } else {
        printf("x is less than or equal to y\n");
    }
    return 0;
}
