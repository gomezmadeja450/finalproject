#include <stdio.h>

int main() {
    int i;

    printf("This is a C program.\n");
    printf("The program uses scanf and printf statements to read and print integers.");
    for (i = 0; i < 5; ++i) {
        printf("%d ", i);
    }
    return 0;
}
